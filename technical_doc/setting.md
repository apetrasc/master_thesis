# 計算環境設定について

本研究室には多数の計算資源がありますが、それらを使わずインターネットでサーバーに接続し、大規模な計算をそこで行うといったことが考えられます。とくに、我々は実験データをファイルサーバであるipomoeaで管理し、また、大規模数値計算をスーパーコンピュータであるWisteriaで実行するような体制を構築しています。
ipomoeaで管理されたデータにアクセスする場合は、Winscpがおススメです。作成したSSHの鍵を写真にあるように登録し、それでもって接続してください。そうすることによって、研究室のNASにダウンロードし、処理できるようになります。
